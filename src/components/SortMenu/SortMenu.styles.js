export default function styles(theme) {
  return {
    container: {
      display: 'flex',
      alignItems: 'center',
    },
    sortByBtn: {
      borderRadius: 0,
      marginLeft: 10,
    },
  }
}
