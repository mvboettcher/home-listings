export default function styles(theme) {
  return {
    root: {
      maxWidth: 420,
      height: 400,
      borderRadius: 0,
      cursor: 'pointer',
    },
    media: {
      height: 240,
    },
  }
}
